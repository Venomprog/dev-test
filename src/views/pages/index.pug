extends ../layouts/master



block content
  +card
  +modal
  +sled 
  +file-card
  .select-wrapper 
    .select
      select.select__select(width='100%')
        each item in ['Италия', 'Япония', 'Германия']
          option(value=item)
            |#{item}